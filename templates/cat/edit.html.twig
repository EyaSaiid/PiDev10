
{% extends 'base2.html.twig' %}

{% block title %}Modifier categorie{% endblock %}

{% block bodyBack %}
    <section class="content">
        <div class="container-fluid">
            <div class="row">
                <!-- left column -->
                <div class="col-md-6">
                    <!-- general form elements -->
                    <div class="card card-primary">
                        <div class="card-header">
                            <h3 class="card-title">modifier une categorie</h3>
                        </div>

                        {{ include('cat/_form.html.twig', {'button_label': 'Update'}) }}

                        <a href="{{ path('cat_index') }}">back to list</a>
                        {{ include('cat/_delete_form.html.twig') }}
                    </div> </div>

    </section>
{% endblock %}
