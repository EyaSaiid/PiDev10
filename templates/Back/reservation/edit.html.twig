{% extends 'base2.html.twig' %}

{% block title %}Modifier une Reservation{% endblock %}

{% block bodyBack %}
<div class="content-wrapper">

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <!-- left column -->
            <div class="col-md-6">
                <!-- general form elements -->
                <div class="card card-primary">
                    <div class="card-header">
                        <h3 class="card-title">Modifier une reservation</h3>
                    </div>
                    <!-- /.card-header -->


                    <div class="card-body">
    {{ include('Back/reservation/_formEdit.html.twig', {'button_label': 'Update'}) }}
                    </div>
                    <div class="card-body">
                        {{ include('Back/reservation/_delete_form.html.twig') }}
                    </div>
    <a href="{{ path('reservation_index') }}">back to list</a>

                </div>
            </div>
        </div>
    </div>
</section>
</div>
{% endblock %}
