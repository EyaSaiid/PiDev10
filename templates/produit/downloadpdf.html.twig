{% extends "base-pdf.html.twig" %}

{% block title %}liste{% endblock %}
{% block body %}

    <p>hellllo word</p>
    <h2>Vos données</h2>
    <table border=1>
        <thead>
        <tr>
            <th scope="col">Nom</th>
            <th scope="col">Prix</th>
        </tr>
        </thead>
        <tbody>
        <tr>
            <td>{{ app.produit.nomProduit }}</td>
            <td>{{ app.produit.prixProduit }}</td>
        </tr>
        </tbody>
    </table>

    <h2>Vos images</h2>
    <h3>{{ app.produit.nomProduit }}</h3>
    {{ absolute_url(asset('upload/img/' ~ app.user.image)) }}

{% endblock %}