{% extends 'base.html.twig' %}

{% block bodyMenu %}
    <!-- Page Header Start -->
    <div class="page-header mb-0">
        <div class="container">
            <div class="row">
                <div class="col-12">
                    <h2>Nos produits</h2>
                </div>
                <div class="col-12">
                    <a href="">Home</a>
                    <a href="">About Us</a>
                </div>
            </div>
        </div>
    </div>
    <!-- Page Header End -->
    <!-- Food Start -->

<div class="food">
    <div class="container">
        <div class="row align-items-center">
            {%  for produit in p %}

                <div class="col-md-4">
                    <div class="food-item">
                          {#  <i class="flaticon-burger"></i>#}
                        <img src="/Front/img/imageMenu2.jpg" alt="Image" width="100" height="100">

                        <h2>{{ produit.nomProduit }}</h2>
                        <p>
                            {{ produit.descriptionProduit }}
                        </p>
                        <h2>
                            {{ produit.prixProduit }} DNT
                        </h2>
                       <bouton class="btn btn-success"> <a href="{{ path('showProduit', {'id': produit.id}) }}" >Voir produit</a></bouton>
                       {# <ul> <bouton class="btn btn-dark"> <a href="{{ path('categorie_front') }}"> revenir a la liste</a></bouton> </ul>
                       #}
                    </div>
                </div>





 {% endfor %}

{% endblock %}